# Goal here:
# Investigate why the prior does not seem to take over 
# When moving away from data, even though
# The uncertainty profile seems to suggest it

# %
# Complete training
# - name: investigate_prior_kl_complete_training
#   experiment_name: mnist_2d
#   model_name: v3ae
#   n_workers: 0
#   # hparams
#   batch_size: 500
#   learning_rate: 1e-4
#   eps: 1e-3
#   n_mc_samples: 3
#   digits:
#     - 2
#     - 5
#   # ood
#   tau_ood: 0.5
#   ood_z_generation_method: gd_aggregate_posterior
#   kde_bandwidth_multiplier: 9
#   prior_α: 1.625
#   prior_β: 0.0010704155012337458
#   # misc
#   n_trials: 1
#   seed: 3
#   max_epochs: 100
#   # pl trainer
#   early_stopping_patience: 100
#   log_every_n_steps: 10 # How often to log within steps (defaults to every 50 steps)
#   flush_logs_every_n_steps: 10 # How often does the log writes to disc
#   progress_bar_refresh_rate: 3 # How often to refresh progress bar (in steps).
#   check_val_every_n_epoch: 10 # Check val every n train epochs

- name: investigate_kl_complete_training
  experiment_name: mnist_2d
  model_name: v3ae
  n_workers: 0
  # hparams
  batch_size: 500
  learning_rate: 1e-4
  eps: 1e-3
  n_mc_samples: 3
  digits:
    - 2
    - 5
  # ood
  tau_ood: 0.
  # ood_z_generation_method: gd_aggregate_posterior
  kde_bandwidth_multiplier: 9
  prior_α: 1.625
  prior_β: 0.0010704155012337458
  # misc
  n_trials: 1
  seed: 3
  max_epochs: 100
  # pl trainer
  early_stopping_patience: 100
  log_every_n_steps: 10 # How often to log within steps (defaults to every 50 steps)
  flush_logs_every_n_steps: 10 # How often does the log writes to disc
  progress_bar_refresh_rate: 3 # How often to refresh progress bar (in steps).
  check_val_every_n_epoch: 10 # Check val every n train epochs


# %
# More informative prior
# - name: investigate_prior_kl_informative_prior
#   experiment_name: mnist_2d
#   model_name: v3ae
#   n_workers: 0
#   # hparams
#   batch_size: 500
#   learning_rate: 1e-3
#   eps: 1e-3
#   n_mc_samples: 3
#   digits:
#     - 2
#     - 5
#   # ood
#   tau_ood: 0.5
#   ood_z_generation_method: gd_aggregate_posterior
#   # kde_bandwidth_multiplier: 9
#   prior_α: 1874
#   prior_β: 3
#   # misc
#   n_trials: 1
#   seed: 3
#   max_epochs: 20
#   # pl trainer
#   early_stopping_patience: 100
#   log_every_n_steps: 10 # How often to log within steps (defaults to every 50 steps)
#   flush_logs_every_n_steps: 10 # How often does the log writes to disc
#   progress_bar_refresh_rate: 3 # How often to refresh progress bar (in steps).
#   check_val_every_n_epoch: 2 # Check val every n train epochs

# # %
# # Higher OOD ratio
# - name: investigate_prior_kl_higher_ood_ratio
#   experiment_name: mnist_2d
#   model_name: v3ae
#   n_workers: 0
#   # hparams
#   batch_size: 500
#   learning_rate: 1e-3
#   eps: 1e-3
#   n_mc_samples: 3
#   digits:
#     - 2
#     - 5
#   # ood
#   tau_ood: 0.9
#   ood_z_generation_method: gd_aggregate_posterior
#   # kde_bandwidth_multiplier: 9
#   prior_α: 1.625
#   prior_β: 0.001
#   # misc
#   n_trials: 1
#   seed: 3
#   max_epochs: 20
#   # pl trainer
#   early_stopping_patience: 100
#   log_every_n_steps: 10 # How often to log within steps (defaults to every 50 steps)
#   flush_logs_every_n_steps: 10 # How often does the log writes to disc
#   progress_bar_refresh_rate: 3 # How often to refresh progress bar (in steps).
#   check_val_every_n_epoch: 2 # Check val every n train epochs
